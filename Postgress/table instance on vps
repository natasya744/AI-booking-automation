version: "3.8"

services:
  postgres:
    image: postgres:15
    container_name: postgres
    restart: always
    
volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - proxy_public

volumes:
  postgres_data:

networks:
    proxy_public: 
      external: true
